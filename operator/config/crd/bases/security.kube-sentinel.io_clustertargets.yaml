---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: clustertargets.security.kube-sentinel.io
spec:
  group: security.kube-sentinel.io
  names:
    kind: ClusterTarget
    listKind: ClusterTargetList
    plural: clustertargets
    shortNames:
    - ct
    singular: clustertarget
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.environment
      name: Environment
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.kubernetesVersion
      name: K8sVersion
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterTarget registers a Biz Cluster as a scan target in the
          Mgmt Cluster.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterTargetSpec defines a Biz Cluster target registered
              in the Mgmt Cluster.
            properties:
              bootstrapPolicy:
                description: |-
                  ClusterTargetBootstrapPolicy declares which preflight-detected missing items
                  the Mgmt operator may auto-install. Items not listed are reported as
                  "install required" but never installed.
                properties:
                  attachImagePullSecretRef:
                    description: LocalObjectRef references another object by name
                      (and optional namespace).
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                    required:
                    - name
                    type: object
                  installManagedRBAC:
                    type: boolean
                  installMissingNamespace:
                    type: boolean
                  installScannerResources:
                    type: boolean
                type: object
              capabilities:
                description: TargetCapabilitySpec declares what kube-sentinel may
                  do against the Biz Cluster.
                properties:
                  hostPath:
                    type: boolean
                  readOnlyInspection:
                    type: boolean
                  scannerJobs:
                    type: boolean
                  trivyOperatorReports:
                    type: boolean
                type: object
              displayName:
                type: string
              environment:
                type: string
              kubeconfigRef:
                description: |-
                  SecretKeyRef references a single key inside a Secret in the Mgmt Cluster.
                  The referenced value (e.g. kubeconfig) is never exposed through API, logs,
                  status, or reports.
                properties:
                  key:
                    type: string
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
              namespaceAllowlist:
                items:
                  type: string
                type: array
              output:
                description: TargetOutputSpec carries report routing for a target.
                properties:
                  reportTenantID:
                    type: string
                type: object
              targetNamespace:
                type: string
            required:
            - kubeconfigRef
            type: object
          status:
            description: ClusterTargetStatus records connection, discovery, and credential
              state.
            properties:
              capabilities:
                description: TargetCapabilityStatus is the observed capability projection
                  after discovery.
                properties:
                  readOnlyInspection:
                    type: boolean
                  scannerJobs:
                    type: boolean
                  trivyOperatorReports:
                    type: boolean
                type: object
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              kubernetesVersion:
                type: string
              lastCredentialRotationAt:
                format: date-time
                type: string
              lastValidatedAt:
                format: date-time
                type: string
              namespaces:
                items:
                  type: string
                type: array
              observedGeneration:
                format: int64
                type: integer
              phase:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
